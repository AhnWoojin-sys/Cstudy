#include <stdio.h>
void fibonacci(int num);

int main(void)
{
    int num;
    printf("Please type A number greater than 1 ");
    scanf("%d", num);
    if(num>1)
    {
        printf("Type A number greater than 1!!");
        return -1;
    }
    fibonacci(num);

    return 0;
}

void fibonacci(int num)
{
    int f1=0, f1=1, f2=1, f3;
    
    if(num == 1)
    

    for(int i=0;i<num;i++)
    {
            
        printf("%d\t", i);
    }
}